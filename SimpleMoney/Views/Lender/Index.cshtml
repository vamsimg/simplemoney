@model IEnumerable<SimpleMoney.Models.Lender>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ACN)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ACN)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LenderID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LenderID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LenderID })
        </td>
    </tr>
}

</table>
